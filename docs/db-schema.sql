-- Script inicial de tablas
-- Este archivo es una referencia. Las migraciones reales se manejan con Prisma.

-- Ejemplo de estructura (ajustar según necesidades reales):

-- CREATE TABLE users (
--   id VARCHAR(255) PRIMARY KEY,
--   email VARCHAR(255) UNIQUE NOT NULL,
--   name VARCHAR(255),
--   role VARCHAR(50) NOT NULL,
--   created_at TIMESTAMP DEFAULT NOW(),
--   updated_at TIMESTAMP DEFAULT NOW()
-- );

-- CREATE TABLE products (
--   id VARCHAR(255) PRIMARY KEY,
--   name VARCHAR(255) NOT NULL,
--   description TEXT,
--   price DECIMAL(10, 2) NOT NULL,
--   stock INTEGER DEFAULT 0,
--   created_at TIMESTAMP DEFAULT NOW(),
--   updated_at TIMESTAMP DEFAULT NOW()
-- );

-- Nota: Usar Prisma migrations para gestionar el esquema de la base de datos

