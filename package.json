{
  "name": "spm-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Sistema de Punto de Venta (SPM) - Monorepo",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/frontend",
    "dev:frontend": "npm run dev --workspace=apps/frontend",
    "dev:backend": "npm run dev --workspace=apps/backend",
    "build": "npm run build --workspace=packages/db && npm run build --workspace=packages/auth && npm run build --workspace=packages/utils && npm run build --workspace=apps/backend && npm run build --workspace=apps/frontend",
    "build:packages": "npm run build --workspace=packages/db && npm run build --workspace=packages/auth && npm run build --workspace=packages/utils",
    "build:frontend": "npm run build --workspace=apps/frontend",
    "build:backend": "npm run build --workspace=apps/backend",
    "lint": "npm run lint --workspaces",
    "test": "npm run test --workspaces",
    "db:generate": "npm run generate --workspace=packages/db",
    "db:migrate": "npm run migrate --workspace=packages/db",
    "db:studio": "npm run studio --workspace=packages/db"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.2"
  }
}
